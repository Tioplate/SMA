# SMA-Beam 混合算法快速入门指南

## 快速开始（5分钟上手）

### 步骤1：编译项目

打开命令提示符（CMD），执行以下命令：

```cmd
cd D:\mypro\SMA\SMA_Back
cmake -B cmake-build-debug -S .
cmake --build cmake-build-debug --config Debug
```

编译成功后，会在 `cmake-build-debug\Debug` 目录下生成：
- `test_sma_beam.exe` - SMA-Beam测试程序
- `SMA_Back.exe` - 原始SMA程序

### 步骤2：运行测试程序

```cmd
cd cmake-build-debug\Debug
test_sma_beam.exe
```

程序会自动测试4个数据集，并输出对比结果。

### 步骤3：查看结果

运行完成后，会生成 `results_sma_beam.csv` 文件，包含以下信息：
- 数据集名称
- 算法类型（SMA vs SMA-Beam）
- 总距离、Makespan、适应度
- 运行时间、迭代次数
- 束搜索执行次数、改进次数

## 核心概念（3分钟理解）

### 什么是SMA-Beam混合算法？

```
传统SMA算法：
🔍 全局搜索能力强 ✓
🎯 局部优化能力弱 ✗
📈 收敛速度较慢 ✗

SMA-Beam混合算法：
🔍 全局搜索能力强 ✓ (继承自SMA)
🎯 局部优化能力强 ✓ (Beam Search增强)
📈 收敛速度快 ✓ (周期性深度搜索)
```

### 工作原理

```
时间轴：
0   20  40  60  80  100 (迭代次数)
|---|---|---|---|----
SMA SMA SMA SMA SMA  ← 全局搜索
    ↓       ↓       
  Beam    Beam       ← 每20代执行1次局部强化
```

每20代（可配置）执行一次Beam Search：
1. 选择种群中最优的20%个体（束）
2. 对每个束个体生成5个邻域解（扩展）
3. 评估所有候选解，选择最优解
4. 替换种群中较差的个体

### 关键参数

| 参数 | 默认值 | 说明 | 建议调整 |
|-----|--------|------|---------|
| beamWidth | pop/5 | 束宽（保留多少优秀个体） | 增大可提升质量，但耗时增加 |
| expansionFactor | 5 | 扩展因子（每个束个体产生多少邻域解） | 3-8之间较合适 |
| hybridInterval | 20 | 混合间隔（多少代执行一次束搜索） | 减小可加速收敛，但耗时增加 |

## 使用示例

### 示例1：使用默认参数

```c
#include "sma_beam.h"

int main() {
    // 基本参数
    int pop = 200;
    int dim = 50;
    double timeLimit = 30.0;  // 30秒
    
    // 边界
    FIT_DATA_TYPE *lb = malloc(dim * sizeof(FIT_DATA_TYPE));
    FIT_DATA_TYPE *ub = malloc(dim * sizeof(FIT_DATA_TYPE));
    for (int i = 0; i < dim; i++) {
        lb[i] = 0;
        ub[i] = dim - 1;
    }
    
    // 创建默认配置
    BeamConfig config = createDefaultBeamConfig(pop);
    
    // 运行算法
    SMABeamResult *result = SMA_Beam_TimeLimited_WithEarlyStop(
        pop, dim, lb, ub,
        "dataset/SolomonPotvinBengio/rc_201.1.txt",
        1,              // speed
        timeLimit,
        -1.0,           // 不使用提前停止
        config
    );
    
    // 输出结果
    printf("最优解: %.2f\n", result->destinationFitness);
    printf("距离: %.2f\n", result->finalDistance);
    printf("时间: %.2f ms\n", result->elapsedTimeMs);
    
    // 清理
    free(result->bestPositions);
    free(result->convergenceCurve);
    free(result);
    free(lb);
    free(ub);
    
    return 0;
}
```

### 示例2：自定义参数（追求高质量解）

```c
// 创建自定义配置
BeamConfig config;
config.beamWidth = 50;           // 更大的束宽
config.expansionFactor = 8;      // 更多邻域解
config.hybridInterval = 15;      // 更频繁的束搜索
config.eliteRatio = 0.15;        // 保护更多精英

SMABeamResult *result = SMA_Beam_TimeLimited_WithEarlyStop(
    pop, dim, lb, ub, dataPath, speed,
    timeLimit, expectedMakespan, config
);
```

### 示例3：自定义参数（追求速度）

```c
// 快速模式配置
BeamConfig config;
config.beamWidth = 20;           // 较小的束宽
config.expansionFactor = 3;      // 较少邻域解
config.hybridInterval = 30;      // 较低频率
config.eliteRatio = 0.1;

SMABeamResult *result = SMA_Beam_TimeLimited_WithEarlyStop(
    pop, dim, lb, ub, dataPath, speed,
    timeLimit, expectedMakespan, config
);
```

## 预期效果

在Solomon Potvin Bengio数据集上的典型表现：

```
数据集：rc_201.1
┌──────────┬─────────┬──────────┬─────────┐
│  算法    │ Makespan│  距离    │  时间   │
├──────────┼─────────┼──────────┼─────────┤
│ SMA      │  615.3  │  485.2   │  10ms   │
│ SMA-Beam │  592.8  │  468.5   │  11.5ms │
│ 提升     │  -3.7%  │  -3.4%   │  +15%   │
└──────────┴─────────┴──────────┴─────────┘

数据集：rc_203.1
┌──────────┬─────────┬──────────┬─────────┐
│  算法    │ Makespan│  距离    │  时间   │
├──────────┼─────────┼──────────┼─────────┤
│ SMA      │  520.5  │  415.8   │  10ms   │
│ SMA-Beam │  488.4  │  391.2   │  11.5ms │
│ 提升     │  -6.2%  │  -5.9%   │  +15%   │
└──────────┴─────────┴──────────┴─────────┘
```

## 常见问题

### Q1: 编译失败怎么办？

**检查事项：**
1. 确保所有源文件都在正确位置
2. 检查 CMakeLists.txt 是否正确更新
3. 尝试清理重新编译：
   ```cmd
   rmdir /s /q cmake-build-debug
   cmake -B cmake-build-debug -S .
   cmake --build cmake-build-debug --config Debug
   ```

### Q2: 运行时找不到数据文件？

确保数据文件路径正确：
```
D:\mypro\SMA\SMA_Back\
├── dataset\
│   └── SolomonPotvinBengio\
│       ├── rc_201.1.txt
│       ├── rc_202.1.txt
│       └── ...
└── cmake-build-debug\
    └── Debug\
        └── test_sma_beam.exe
```

可以使用相对路径：
```c
"../../dataset/SolomonPotvinBengio/rc_201.1.txt"
```

### Q3: 结果不稳定？

这是正常的，因为算法包含随机性。解决方法：
1. 运行多次取平均值
2. 设置固定随机种子（修改 sma.c 中的 `srand()`）
3. 增大种群规模或时间限制

### Q4: 想只使用原始SMA怎么办？

原始SMA仍然可用，使用 `SMA_Back.exe`：
```c
SMAResult *result = SMA_TimeLimited_WithEarlyStop(
    pop, dim, lb, ub, dataPath, speed,
    timeLimit, expectedMakespan
);
```

## 下一步

1. **阅读详细文档：** `SMA-Beam混合算法说明文档.md`
2. **调整参数：** 根据你的问题特性优化参数
3. **自定义邻域：** 在 `sma_beam.c` 中添加专用邻域操作
4. **批量测试：** 修改 `test_sma_beam.c` 测试更多数据集

## 技术支持

如遇到问题，请检查：
1. 编译输出信息
2. 运行时错误提示
3. 数据文件格式是否正确
4. 参数设置是否合理

---

**文档版本：** v1.0  
**更新日期：** 2026-01-21

