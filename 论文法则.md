这篇论文的“四步收紧”和“三步剔除”是其能够实现极速剪枝的核心引擎。这两个过程在一个循环中不断交替运行，直到没有任何更新为止 。

这里为你极其详细地拆解它们的物理意义和数学逻辑。其中，$a_{i,j}(t)$ 代表在 $t$ 时刻离开 $i$ 抵达 $j$ 的时间 ，$a_{i,j}^{-1}(t)$ 代表为了在 $t$ 时刻抵达 $j$，最晚必须离开 $i$ 的时间 。

### 一、 四步收紧法则 (Tightening TWs)

这四步的核心逻辑是：**利用前驱节点和后继节点的物理极限，挤干当前节点 $k$ 时间窗 $[e_k, l_k]$ 里的水分。**

* **第一步：基于前驱推迟最早时间 (Forward push of $e_k$)**
* **物理意义**：如果你的上游节点们就算拼了命（在它们的最早时间 $e_i$ 出发），能抵达你这里的最快时间依然比你目前设定的 $e_k$ 还要晚，那你的 $e_k$ 就不切实际，必须推迟。
* 
**公式**：$e_{k} \leftarrow \max\{e_{k}, \min_{(i,k)\in\mathcal{E}}a_{i,k}(e_{i})\}$ 


* 
**操作**：遍历所有能到达 $k$ 的合法前驱节点 $i$，算出最快到达时间，取其最小值。如果这个最小值比 $e_k$ 大，就更新 $e_k$ 。




* **第二步：基于后继推迟最早时间 (Backward push of $e_k$)**
* **物理意义**：如果你在 $e_k$ 时刻早早地访问了节点 $k$，但这样做会导致你接下来去**所有**合法的后继节点 $i$ 时，都去得太早了，只能原地傻等（必须等到它们的 $e_i$），那不如把 $k$ 的最早访问时间往后推，避免无意义的等待。
* 
**公式**：$e_{k} \leftarrow \max\{e_{k}, \min_{(k,i)\in\mathcal{E}}a_{k,i}^{-1}(e_{i})\}$ 


* 
**操作**：算出为了准时赶上每个后继节点 $i$ 的开门时间 $e_i$，你必须离开 $k$ 的最晚时间，取其中的最小值。如果这个值大于 $e_k$，推迟 $e_k$ 。




* **第三步：基于后继提前最晚时间 (Backward pull of $l_k$)**
* **物理意义**：如果你拖到当前的 $l_k$ 才离开节点 $k$，会导致你接下去无论去**哪个**合法的后继节点 $i$，都会迟到（超过它们的 $l_i$），说明你的 $l_k$ 定得太晚了，必须提前。
* 
**公式**：$l_{k} \leftarrow \min\{l_{k}, \max_{(k,i)\in\mathcal{E}}a_{k,i}^{-1}(l_{i})\}$ 


* 
**操作**：如果 $l_k$ 意味着去所有后续节点都会迟到，就把它提前，缩紧最晚时间 。




* **第四步：基于前驱提前最晚时间 (Forward pull of $l_k$)**
* **物理意义**：哪怕你所有的前驱节点 $i$ 都拖到它们允许的最后期限 $l_i$ 才出发，它们抵达 $k$ 的时间依然早于你设定的 $l_k$，说明 $l_k$ 太宽松了，实际上根本不可能拖到这么晚，必须将其提前。
* 
**公式**：$l_{k} \leftarrow \min\{l_{k}, \max_{(i,k)\in\mathcal{E}}a_{i,k}(l_{i})\}$ 


* 
**操作**：计算所有前驱节点最晚出发抵达 $k$ 的时间，取最大值，用来缩紧 $l_k$ 。





---

### 二、 三步网络剔除法则 (Filtering $\mathcal{E}$ and updating $\mathcal{R}$)

这三步的核心逻辑是：**利用刚刚收紧的时间窗，找出绝对不可能走的边（从 $\mathcal{E}$ 剔除），并确立绝对的先后顺序（加入 $\mathcal{R}$）。**

* **第一步：直接超时断边**
* 
**判定**：如果 $a_{i,j}(e_{i}) > l_{j}$ 。即：即使你以最快速度 $e_i$ 从 $i$ 出发，抵达 $j$ 依然会迟到（大于 $l_j$）。


* 
**动作**：将边 $(i, j)$ 从集合 $\mathcal{E}$ 中剔除 。同时，既然 $i$ 无法走到 $j$，为了完成任务，必须确立绝对顺序：$j$ 必须在 $i$ 前面完成，将 $(j, i)$ 加入集合 $\mathcal{R}$ 。




* **第二步：三节点子路径校验 (Subpath checking)**
* 
**定义不可行**：如果一条由三个节点组成的路径 $(i, j, k)$ 会导致在 $j$ 迟到，或者在 $k$ 迟到，那么这条子路径就是不可行的 。（即：$a_{i,j}(e_{i}) > l_{j}$ 或 $a_{j,k}(\max\{e_{j}, a_{i,j}(e_{i})\}) > l_{k}$ ）。


* 
**动作 1**：对于某个节点 $j$，如果能找到**哪怕一个**节点 $k$，使得子路径 $(i, j, k)$ 和 $(k, i, j)$ 都不可行。说明无论如何 $i$ 和 $j$ 不能直接连在一起。此时，将边 $(i, j)$ 从 $\mathcal{E}$ 中剔除 。


* 
**动作 2**：在动作 1 的基础上，如果连 $(i, k, j)$ 这条子路径也是不可行的。说明 $i$ 连排在 $j$ 前面的资格都没有（中间插一个 $k$ 都不行）。此时，将绝对顺序 $(j, i)$ 加入集合 $\mathcal{R}$ 。




* **第三步：推移闭包与逆向剔除**
* 
**推移闭包动作**：如果集合 $\mathcal{R}$ 中已经存在了“$i$ 必须在 $j$ 前”且“$j$ 必须在 $k$ 前”（即 $\{(i, j), (j, k)\} \subseteq \mathcal{R}$），则推导出“$i$ 必须在 $k$ 前” 。将 $(i, k)$ 加入 $\mathcal{R}$ 。并且，既然中间必定隔着一个 $j$，说明 $i$ 不可能直接连向 $k$，将边 $(i, k)$ 从 $\mathcal{E}$ 中剔除 。


* 
**逆向剔除动作**：对于 $\mathcal{R}$ 中存在的每一个绝对顺序 $(i, j)$，意味着必须先 $i$ 后 $j$ 。因此，直接从 $j$ 返回 $i$ 是绝对违法的。将反向边 $(j, i)$ 从集合 $\mathcal{E}$ 中剔除 。